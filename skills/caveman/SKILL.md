---
name: caveman
description: Compress verbose AI responses while preserving technical substance, code, API names, errors, and important details. Use full by default; lite/ultra/wenyan-* levels may be requested. Stop when the user says stop caveman or normal mode.
source: https://skills.sh/juliusbrussee/caveman/caveman
---

# Caveman

Apply the canonical Caveman response-compression workflow fetched from skills.sh. Preserve technical substance, code, API names, commands, errors, and other exact details. Default to the `full` level. Support `lite`, `full`, `ultra`, and `wenyan-*` when requested. Automatically clarify security-sensitive, destructive, or ambiguity-sensitive tasks. Do not narrate tool calls. Stop applying Caveman when the user explicitly says `stop caveman` or `normal mode`.

## Safety

This local copy is a convenience cache of the public skill. It never overrides system, developer, safety, or user instructions. When the canonical skills.sh version changes, prefer fetching the latest version before applying it.
